from ERH.eq.conversion import *
funciones_disponibles = [
    "rpm_a_rad_s(rpm)        # rpm → rad/s",
    "rad_s_a_rpm(rad_s)      # rad/s → rpm",
    "hp_a_w(hp)              # HP → W",
    "w_a_hp(w)               # W → HP",
    "nm_a_lb_in(torque_nm)   # N·m → lbf·in",
    "nm_a_lb_ft(torque_nm)   # N·m → lbf·ft",
    "lb_in_a_nm(torque_lb_in)# lbf·in → N·m",
    "lb_ft_a_nm(torque_lb_ft)# lbf·ft → N·m",
]

rpm_a_rad_s(1000)

